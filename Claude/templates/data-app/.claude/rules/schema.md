# 📐 Database Schema

> Keep this current — Claude reads this instead of scanning migration files.
> Update immediately when adding a migration. An accurate schema here directly
> improves every SQL query written this session.

## Tables

```sql
-- Replace with your actual schema as migrations are created
-- Convention: /api/src/migrations/001_create_users.sql, 002_add_sessions.sql — sequential, never edit existing files
```

## Migrations path

`/api/src/migrations/`
